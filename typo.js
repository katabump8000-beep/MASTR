// ============================================================
// typo.js
// ALJESAT BOT
// تصحيح الأخطاء الإملائية الشائعة في الأوامر
// ============================================================

"use strict";

// ============================================================
// أدوات مساعدة
// ============================================================

function normalizeText(text) {
    if (text === null || text === undefined) return "";
    return String(text)
        .trim()
        .replace(/[أإآ]/g, "ا")
        .replace(/ى/g, "ي")
        .replace(/ة/g, "ه")
        .replace(/\s+/g, " ")
        .toLowerCase();
}

async function safeSend(sock, jid, content, options = {}) {
    if (!sock || !jid) return Promise.resolve(null);
    return sock.sendMessage(jid, content, options).catch(() => null);
}

// ============================================================
// 🗺️ خريطة التصحيحات
// مفتاح: الشكل المُصحَّح (بعد normalize) → الأمر الصحيح
// ============================================================

const TYPO_MAP = {
    // ============ تفاصيلي ============
    "تفاصيل": "تفاصيلي",
    "تفاصيلي": "تفاصيلي",
    "تفاصلي": "تفاصيلي",
    "تفصيلي": "تفاصيلي",
    "معلوماتي": "تفاصيلي",
    "بياناتي": "تفاصيلي",
    "بروفايلي": "تفاصيلي",
    "بروفايل": "تفاصيلي",

    // ============ تفاصيله ============
    "تفاصيله": "تفاصيله",
    "تفاصله": "تفاصيله",
    "معلوماته": "تفاصيله",
    "بياناته": "تفاصيله",

    // ============ رتبتي ============
    "رتبتي": "رتبتي",
    "رتبه": "رتبتي",
    "رتبت": "رتبتي",
    "منصبي": "رتبتي",

    // ============ رصيد ============
    "رصيد": "رصيد",
    "رصيدي": "تفاصيلي",
    "فلوسي": "تفاصيلي",
    "راسمالي": "تفاصيلي",

    // ============ القاب ============
    "القاب": "القاب",
    "الالقاب": "القاب",
    "لقاب": "القاب",
    "قائمة الالقاب": "القاب",

    // ============ هدية ============
    "هدية": "هدية",
    "هديه": "هدية",
    "جائزة": "هدية",
    "جايزة": "هدية",

    // ============ مخزوني ============
    "مخزوني": "مخزوني",
    "مخزنى": "مخزوني",
    "حقيبتي": "مخزوني",
    "جبي": "مخزوني",

    // ============ الفعاليات ============
    "العاب": "العاب",
    "الالعاب": "العاب",
    "العبن": "العاب",
    "فعاليات": "العاب",
    "فعالية": "العاب",

    // ============ تفكيك ============
    "تفكيك": "تفكيك",
    "تفكيك الكلمات": "تفكيك",

    // ============ كتابة ============
    "كتابة": "كتابة",
    "كتابه": "كتابة",

    // ============ اعلام ============
    "اعلام": "اعلام",
    "الأعلام": "اعلام",
    "علم": "اعلام",

    // ============ ايموجي ============
    "ايموجي": "ايموجي",
    "إيموجي": "ايموجي",
    "ايموجى": "ايموجي",
    "ايموجيات": "ايموجي",

    // ============ الوان ============
    "الوان": "الوان",
    "الألوان": "الوان",
    "لون": "الوان",
    "ألوان": "الوان",

    // ============ الحيوانات ============
    "الحيوانات": "الحيوانات",
    "حيوانات": "الحيوانات",
    "الحيوان": "الحيوانات",

    // ============ تخمين ============
    "تخمين": "تخمين",
    "التخمين": "تخمين",
    "خمن": "تخمين",

    // ============ صراحة ============
    "صراحة": "صراحة",
    "صراحه": "صراحة",
    "الصراحة": "صراحة",

    // ============ كازينو ============
    "كازينو": "كازينو",
    "الكازينو": "كازينو",
    "كازينوه": "كازينو",

    // ============ روليت ============
    "روليت": "روليت",
    "الروليت": "روليت",
    "روليتي": "روليت",

    // ============ كريستال ============
    "كريستال": "كريستال",
    "الكرستال": "كريستال",
    "الكرستاله": "كريستال",

    // ============ مزاد ============
    "مزاد": "مزاد",
    "المزاد": "مزاد",
    "مزادي": "مزاد",

    // ============ ابلاغ ============
    "ابلاغ": "ابلاغ",
    "الإبلاغ": "ابلاغ",
    "شكوى": "ابلاغ",
    "بلاغ": "ابلاغ",

    // ============ رتبته ============
    "رتبته": "رتبته",
    "رتبة العضو": "رتبته",

    // ============ علاقة ============
    "علاقة": "علاقة",
    "علاقه": "علاقة",
    "صديق": "علاقة",
    "صديقي": "علاقة",

    // ============ تحويل ============
    "تحويل": "تحويل",
    "حول": "تحويل",
    "حول الى": "تحويل",

    // ============ اوامر ============
    "اوامر": "اوامر",
    "الأوامر": "اوامر",
    "الاوامر": "اوامر",
    "الأمر": "اوامر",
    "قائمة الاوامر": "اوامر",

    // ============ نتائج ============
    "نتائج": "نتائج",
    "النتائج": "نتائج",
    "نتيجة": "نتائج",

    // ============ كلمة سرية إضافية ============
    "المطور": "المطور",
    "مطور": "المطور"
};

// ============================================================
// استخراج الأمر المصحح
// ============================================================

function getCorrectedCommand(text) {
    if (!text) return null;

    // إزالة النقطة والبادئات
    let cleaned = String(text).trim();
    
    if (cleaned.startsWith(".")) {
        cleaned = cleaned.slice(1).trim();
    }

    if (!cleaned) return null;

    // تنظيف النص
    const normalized = normalizeText(cleaned);

    // البحث المباشر في الخريطة
    if (TYPO_MAP[normalized]) {
        return "." + TYPO_MAP[normalized];
    }

    // البحث بالكلمات المفتاحية (إذا النص يحتوي على كلمة معروفة)
    for (const [key, cmd] of Object.entries(TYPO_MAP)) {
        if (normalized === key) {
            return "." + cmd;
        }
        // إذا النص يبدأ بالكلمة
        if (normalized.startsWith(key + " ")) {
            const rest = cleaned.slice(key.length).trim();
            return "." + cmd + (rest ? " " + rest : "");
        }
    }

    return null;
}

// ============================================================
// 🎯 المعالجة الرئيسية
// ============================================================

/**
 * يحاول تصحيح الأمر إذا كان مشابهاً لأمر موجود
 * @returns {Boolean} true إذا تم التعامل مع الرسالة (تصحيح + تنفيذ)
 */
async function handleTypo(sock, jid, msg, text, db, cleanSender, isBotOwner) {
    try {
        if (!text) return false;

        // إذا الرسالة تبدأ بنقطة، لا نعالجها هنا (handleCommand سيتعامل معها)
        if (String(text).startsWith(".")) return false;

        // محاولة التصحيح
        const corrected = getCorrectedCommand(text);
        if (!corrected) return false;

        // إرسال الأمر المصحح إلى handleCommand
        try {
            const { handleCommand } = require("./commands");

            const fakeMsg = {
                ...msg,
                message: { conversation: corrected }
            };

            const handled = await handleCommand(sock, jid, fakeMsg, {
                db,
                sender: msg?.key?.participant || msg?.key?.remoteJid,
                cleanSender,
                isGroup: true,
                isBotOwner: Boolean(isBotOwner),
                botNumber: cleanSender,
                text: corrected
            });

            return handled !== false;

        } catch (err) {
            console.error("❌ typo exec error:", err?.message);
            return false;
        }

    } catch (error) {
        console.error("❌ خطأ في handleTypo:", error?.message || error);
        return false;
    }
}

// ============================================================
// تصدير
// ============================================================

module.exports = {
    handleTypo,
    getCorrectedCommand,
    TYPO_MAP,
    normalizeText
};
// ============================================================
// commands_list.js
// ALJESAT BOT
// قائمة الأوامر (.اوامر) - تختلف حسب القروب
// ============================================================

"use strict";

// ============================================================
// أدوات مساعدة
// ============================================================

function cleanNumber(value) {
    if (!value) return "";
    return String(value).replace(/[^0-9]/g, "");
}

async function safeSend(sock, jid, content, options = {}) {
    if (!sock || !jid) return Promise.resolve(null);
    return sock.sendMessage(jid, content, options).catch(() => null);
}

/**
 * فحص هل المستخدم لديه صلاحية إدارية (1/2/3/4)
 */
function hasAdminPerm(db, cleanSender, isBotOwner) {
    if (isBotOwner) return true;

    const permissions = db.permissions || {};
    for (const level of ["1", "2", "3", "4"]) {
        if (Array.isArray(permissions[level]) && permissions[level].includes(cleanSender)) {
            return true;
        }
    }
    return false;
}

/**
 * فحص هل القروب هو ADS
 */
function isAdsGroup(db, jid) {
    return Boolean(db.adsGroups && db.adsGroups[jid] === true);
}

/**
 * فحص هل القروب هو الأساسي
 */
function isMainGroup(db, jid) {
    return Boolean(db.mainGroup && db.mainGroup[jid] === true);
}

// ============================================================
// قائمة الأوامر القصيرة (لقروب ADS / الأساسي)
// ============================================================

const SHORT_COMMANDS_LIST = `_*❉▬▬▬▬اوامر▬▬▬▬❉*_
  ملاحـــ📤ــظة: عزيزي هناك اوامر
 لا يمكنني ارسالها في هذا القروب
\`اما الاوامر المخصصة للاعضاء:\`

\`.تفاصيلي\`
\`.رتبتي\`
\`.القاب\`
\`.رصيد\`
\`.تحويل الى (اللقب) (المبلغ)\`
\`.هدية\`
\`.مخزوني\`
\`.علاقة (لقب1) مع (لقب2)\`
\`.تفاصيله\` @user
\`.تخمين\`
\`.الحيوانات\`
\`.الوان\`
\`.صراحة\`
\`.روليت\`
\`.الكرستال\`
\`.العاب\`
\`.كازينو\`
\`.مزاد\`
\`.ادفع (المبلغ)\`
\`.رهان (المبلغ)\`
\`.بدأ الرهان\`
\`.انسحاب\`
\`.ابلاغ (النص)\`
\`.أوامر\`
\`.نتائج\`

_*✥▬▬▬▬اوامر▬▬▬▬✥*_`;

// ============================================================
// قائمة الأوامر الكاملة (لقروب آخر)
// ============================================================

const FULL_COMMANDS_LIST = `_*❆━━━━═⏣⊰اوامر⊱⏣═━━━━❆*_

*📋 الأوامر العامة:*
\`.تفاصيلي\` - عرض بياناتك
\`.تفاصيله @user\` - عرض بيانات عضو
\`.رتبتي\` - عرض رتبتك
\`.القاب\` - قائمة الألقاب المسجلة
\`.رصيد\` - عرض الرصيد
\`.هدية\` - الهدية اليومية
\`.تحويل الى (لقب) (مبلغ)\` - تحويل رصيد
\`.ابلاغ (النص)\` - إرسال بلاغ
\`.أوامر\` - قائمة الأوامر
\`.مخزوني\` - عرض مخزونك
\`.علاقة (لقب1) مع (لقب2)\` - ربط علاقة

*🎮 الفعاليات:*
\`.العاب\` - قائمة الفعاليات
\`.تفكيك\` - لعبة التفكيك
\`.كتابة\` - لعبة الكتابة
\`.اعلام\` - لعبة الأعلام
\`.ايموجي\` - لعبة الإيموجي
\`.الوان\` - لعبة الألوان
\`.الحيوانات\` - لعبة الحيوانات
\`.تخمين\` - لعبة التخمين
\`.صراحة\` - لعبة الصراحة

*🎰 الكازينو:*
\`.كازينو\` - قائمة الكازينو
\`.روليت\` - لعبة الروليت
\`.الكرستال\` - لعبة الكريستال
\`.رهان (مبلغ)\` - وضع رهان
\`.بدأ الرهان\` - بدء الروليت
\`.انسحاب\` - إلغاء الفعالية

*🎤 المزاد:*
\`.مزاد\` - بدء المزاد
\`.ادفع (مبلغ)\` - وضع عرض
\`.مخزوني\` - عرض المخزون

*⚙️ الإدارة:*
\`.سجل @user (اللقب)\` - تسجيل عضو
\`.حذف (اللقب)\` - حذف لقب
\`.طرف @user (الطرف)\` - إرسال طرف
\`.رصيد @user (مبلغ)\` - إيداع رصيد
\`.سحب @user (مبلغ)\` - سحب رصيد
\`.رتبته @user (الرتبة)\` - تعديل رتبة
\`.تفاعله @user (الرقم)\` - تعديل تفاعل
\`.حسبة\` - حفظ التفاعل
\`.حسبة on/off\` - تفعيل العدّاد

*🛡️ الحماية:*
\`.حماية on/off\` - حماية البطاقات
\`.استقبال on/off\` - تفعيل الاستقبال
\`.مراقبة on/off\` - المراقبة العامة
\`.تفاعل on/off\` - التفاعل التلقائي
\`.ردود on/off\` - الردود التلقائية
\`.احا on/off\` - أمر احا
\`.هدوء on/off\` - وضع الهدوء
\`.تنظيم on/off\` - مراقبة المغادرين

*🎯 قروبات خاصة:*
\`.اساسي on/off\` - تعيين القروب الأساسي
\`.رابط 1 (URL)\` - رابط الإعلانات
\`.رابط 2 (URL)\` - رابط المتجر
\`.صورة (اللقب)\` - حفظ صورة عضو
\`.نتائج\` - عرض النتائج
\`.نتائج 0\` - تصفير النتائج

*💾 أخرى:*
\`.تنظيف\` - حذف 40 رسالة
\`.حفظ on/off\` - الحفظ التلقائي
\`.استراحة\` - إيقاف الفعاليات
\`.وقف\` - إيقاف جميع الفعاليات

_*❆━━━━═⏣⊰اوامر⊱⏣═━━━━❆*_`;

// ============================================================
// المعالجة الرئيسية
// ============================================================

async function handleCommandsList(sock, jid, msg, db, cleanSender, isBotOwner) {
    try {
        // ============================================
        // 1) التحقق من الصلاحيات
        // ============================================
        if (!hasAdminPerm(db, cleanSender, isBotOwner)) {
            await safeSend(sock, jid, {
                text: "⛔ هذا الأمر يحتاج صلاحيات إدارية (.سماح)."
            }, { quoted: msg });
            return true;
        }

        // ============================================
        // 2) تحديد القائمة حسب نوع القروب
        // ============================================
        const isAds = isAdsGroup(db, jid);
        const isMain = isMainGroup(db, jid);

        let text;

        if (isAds || isMain) {
            // قائمة مختصرة
            text = SHORT_COMMANDS_LIST;
        } else {
            // قائمة كاملة
            text = FULL_COMMANDS_LIST;
        }

        // ============================================
        // 3) إرسال الرسالة
        // ============================================
        await safeSend(sock, jid, { text }, { quoted: msg });

        return true;

    } catch (error) {
        console.error("❌ خطأ في handleCommandsList:", error?.message || error);
        return false;
    }
}

// ============================================================
// تصدير
// ============================================================

module.exports = {
    handleCommandsList,
    hasAdminPerm,
    isAdsGroup,
    isMainGroup,
    SHORT_COMMANDS_LIST,
    FULL_COMMANDS_LIST
};